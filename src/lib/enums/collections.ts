export enum Collections {
  Users = 'users',
  Projects = 'projects',
}

export enum StoreCollections {
  Settings = 'settings',
  Categories = 'categories',
  Contact = 'contact',
  Clients = 'clients',
  Orders = 'orders',
  
}